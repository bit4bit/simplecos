<%= form_for(@consumers_request_cash) do |f| %>
  <% if @consumers_request_cash.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consumers_request_cash.errors.count, "error") %> prohibited this consumers_request_cash from being saved:</h2>

      <ul>
      <% @consumers_request_cash.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :client_id %><br />
    <%= current_consumers_client.name%>
    <%= f.hidden_field :client_id, :value => current_consumers_client.id%>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
