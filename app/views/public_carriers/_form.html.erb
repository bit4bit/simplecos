<%= simple_form_for(@public_carrier) do |f| %>
  <% if @public_carrier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@public_carrier.errors.count, "error") %> prohibited this public_carrier from being saved:</h2>

      <ul>
      <% @public_carrier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.association :sip_profile%>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
