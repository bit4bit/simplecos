<h1><%= I18n.t('sip_clients.edit.title')%></h1>

<%= render 'form' %>

<%= link_to I18n.t('show'), consumers_sip_client_path(@sip_client) %> |
<%= link_to I18n.t('back'), consumers_sip_clients_path(:client_id => @sip_client.client.id) %>
